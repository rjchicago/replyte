{
  "name": "replyte-server",
  "version": "1.0.0",
  "description": "Replyte web application and API",
  "main": "server.js",
  "scripts": {
    "start": "knex migrate:latest && node server.js",
    "dev": "knex migrate:latest && nodemon server.js",
    "debug": "knex migrate:latest && nodemon --inspect=0.0.0.0:9229 server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "knex": "^3.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}